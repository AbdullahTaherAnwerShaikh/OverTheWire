# Level 10 → 11

## Goal
The password for the next level is stored in the file data.txt, which contains base64 encoded data.

## Commands
man, grep, sort, uniq, strings, base64, tr, tar, gzip, bzip2, xxd

base64 -d data.txt

## Learned
- Using base64 to decode data
